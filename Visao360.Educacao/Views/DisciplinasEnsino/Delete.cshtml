@model Dardani.EDU.Entities.Model.DisciplinaEnsino

@{
    ViewBag.Title = "Confirmar Exclusão";
}

<h3>Deseja realmente excluir a Disciplina abaixo?</h3>
@using (Html.BeginForm()) {
    @Html.ValidationSummary(false)
    <fieldset>
        <legend>Detalhes</legend>
        <table style="width:100%;">
            <tr>
                <td style="width:100px;">
                    <div class="editor-label">Disciplina</div>
                </td>
                <td>
                    <div class="editor-field">@Html.DisplayFor(model => model.Disciplina.Descricao)</div>
                </td>
            </tr>
            <tr>
                <td style="width:100px;">
                    <div class="editor-label">Ensino</div>
                </td>
                <td>
                    <div class="editor-field">@Html.DisplayFor(model => model.Ensino.Descricao)</div>
                </td>
            </tr>
        </table>
        @Html.HiddenFor(model => model.Id)
        <p>
            <input type="submit" value="Confirmar" />
        </p>
    </fieldset>
}
<div>
    @Html.ActionLink("Voltar a Lista", "Index", "DisciplinaEnsino", null, new { @class="links_big"})
</div>
