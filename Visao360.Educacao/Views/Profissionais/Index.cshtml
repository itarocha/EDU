@model IEnumerable<Dardani.EDU.Entities.VO.PessoaVO>

@{
    ViewBag.Title = "Listagem de Profissionais";
}

<div class="row">
    <div class="col-md-12">
    <a href="/Profissionais/Edit" class="btn btn-default">Novo Profissional</a>
    </div>
</div>
<!--
 _using (Ajax.BeginForm(    
     new AjaxOptions
     {
         HttpMethod = "get",
         InsertionMode = InsertionMode.Replace,
         UpdateTargetId = "lista"
        }))
     {
         <p>
         Entre com o texto:
         <input type="search" name="SearchString" /> 
         <input type="submit" value="Filtrar" />
         </p> 
     } 
-->
<div id="lista">
@Html.Partial("_Listagem", Model)
</div>
